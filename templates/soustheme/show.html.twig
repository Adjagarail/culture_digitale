{% extends 'base.html.twig' %}

{% block title %}Soustheme{% endblock %}

{% block body %}
    <h1>Soustheme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soustheme.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ soustheme.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('soustheme_index') }}">back to list</a>

    <a href="{{ path('soustheme_edit', {'id': soustheme.id}) }}">edit</a>

    {{ include('soustheme/_delete_form.html.twig') }}
{% endblock %}
